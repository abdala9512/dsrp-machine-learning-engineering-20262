# Module 3 — Series de Tiempo (+ MLflow)
# Core scientific stack
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7

# Modelos estadísticos: STL, suavizamiento exponencial, SARIMA
statsmodels>=0.14

# Pronóstico con ML y ensambles
xgboost>=2.0

# Clustering DTW (notebook 6)
tslearn>=0.6

# Experiment tracking & model registry (MLflow 3.x) — como en el Módulo 2
mlflow>=3.1
psycopg2-binary>=2.9
dagshub>=0.3          # backend cloud: dagshub.init(..., mlflow=True)

# Opcional: auto_arima (notebook 2); puede forzar downgrade de numpy
# pmdarima>=2.0

# Notebook environment / tooling
jupyterlab>=4.0
nbformat>=5.9
