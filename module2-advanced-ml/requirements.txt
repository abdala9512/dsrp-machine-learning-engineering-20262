# Module 2 — Advanced ML Algorithms (+ MLflow)
# Core scientific stack
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12

# Gradient boosting libraries
xgboost>=2.0
lightgbm>=4.0
catboost>=1.2

# Dimensionality reduction
umap-learn>=0.5

# Deep learning (CPU build of PyTorch)
torch>=2.0

# Experiment tracking & model registry (MLflow 3.x)
mlflow>=3.1
psycopg2-binary>=2.9
dagshub>=0.3          # backend cloud: dagshub.init(..., mlflow=True)

# Notebook environment / tooling
jupyterlab>=4.0
nbformat>=5.9
